// This is a simple wrapper script to run the mcp-server-time package
// The actual server is run using 'uvx mcp-server-time'

console.log("Starting MCP Time Server...");
console.log("This script would normally start the MCP server.");
console.log("In this case, we will use 'uvx mcp-server-time' to run the server.");
console.log("See package.json for the actual command.");

// This script doesn't actually do anything, as the server is run using the command
// defined in package.json scripts.